---
import ToolPage from '../../../components/pages/ToolPage.astro';
import { getToolStaticPaths } from '../../../lib/toolRoutes';
import { getTool } from '../../../tools/registry';

export const getStaticPaths = getToolStaticPaths;

const lang = 'zh';
const tool = getTool(Astro.params.tool)!;
---

<ToolPage lang={lang} tool={tool} />
